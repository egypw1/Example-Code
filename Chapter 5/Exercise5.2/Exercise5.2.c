#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    // Declare some variables
    int a = 1, b = 2;
    char c = 2;
    float f = 1.23f, g = 3.141516f;

    // Use printf display text on the screen
    printf ("The values of the variables are... \na = %d, b = %d, c = %c, f = %f, g = %f", a, b, c, f, g);

    // Exit from main
    return 0;
}